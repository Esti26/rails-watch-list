<%= simple_form_for ([@list, @bookmark]) do |f|%>
 <%= f.input :comment %>
 <%= f.input :photo, as: :file %>
 <%= f.association :movie %>
<%= f.submit :class => "btn btn-success btn-mini" %>
<% end %>
